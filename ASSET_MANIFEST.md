# Asset Manifest

No generated, raster, external, geographic, or evidentiary assets are used.

The UI uses CSS layout, the MIT-licensed `lucide-react` package for familiar control/status icons, and an inline SVG line chart built exclusively from the live API trajectory. The chart is computation output, not a static or generated asset. This project deliberately avoids imagery that could imply real city geography or empirical disaster evidence.
