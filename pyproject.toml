[project]
name = "city-recovery-planner"
version = "0.3.0"
description = "Deterministic synthetic city recovery comparison service"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi==0.116.1",
  "gymnasium==1.2.1",
  "numpy==2.3.2",
  "onnx==1.19.0",
  "onnxruntime==1.23.0",
  "ortools==9.14.6206",
  "pydantic==2.11.7",
  "stable-baselines3==2.7.0",
  "torch==2.8.0",
  "uvicorn[standard]==0.35.0",
]

[dependency-groups]
dev = [
  "httpx==0.28.1",
  "pytest==8.4.1",
  "ruff==0.12.4",
]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
addopts = "-q"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
