# Demo Script

## Three-Minute Presentation Walkthrough

1. Start at `http://127.0.0.1:4117`. Point out `Local deterministic PPO`, `ONNX / PCG64`, and `Synthetic model`. Describe the candidate precisely as the frozen SB3 PPO exported to checksum-pinned ONNX CPU inference; do not imply municipal or empirical evidence.
2. In Recovery envelope, show the five ordered services, initial state/priority, seed `424242`, 14 days, 180 daily units, 20% shock chance, and the forced day-5 utility failure. On the fixed-height desktop desk, the dispatch action remains visible while the bounded controls scroll.
3. Run comparison. Read the live candidate and visible OR-Tools GLOP resilience AUC values and the measured percentage-point delta from the run brief. State that these are results for this authored synthetic scenario, not a city forecast.
4. Use the shared-shock chart guides and selected-day line to connect the common shock tape to day 5. In the folio, show the same forced utility shock and available budget, paired candidate/baseline service end states, both allocations, and their projection distances.
5. Point to the independently labelled candidate and baseline measured constraint counts. Open Daily audit to show that all 14 daily records are returned, then use Arrow Left/Right on the result tabs or scrub another day.
6. Change one service value or seed and rerun. The shock hash and trajectory change through real local API computation. Restore the saved result to show that its authored scenario and evidence return together.
7. Close on the footer: the shock-tape and ONNX hash prefixes remain visible beside the synthetic/non-empirical disclosure. The accepted linear artifact is explicitly legacy, not PPO, and not used for production inference.

## Automated Judge Path

Run `scripts/verify.ps1 -Profile cpu` for the bounded functional verifier. It executes the test/build suite, starts the compiled app, submits a different 11-day fixture five times, compares canonical bytes, checks every daily cap/sum and shared shock identity, tests invalid input, and stops the process. This is functional evidence; it does not replace the independent Presentation or Release matrices.
